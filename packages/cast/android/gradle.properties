RnMediaCast_kotlinVersion=2.2.0
# Cast framework 22.x declares minSdk 23; this package keeps the repo-wide
# floor of 24 (same as @afkcodes/timbre-player), so the framework's own floor is
# never the binding constraint.
RnMediaCast_minSdkVersion=24
RnMediaCast_targetSdkVersion=35
RnMediaCast_compileSdkVersion=37
RnMediaCast_ndkVersion=27.1.12297006
# Official Google Cast sender SDK (the Android half of the pair pinned in
# RnMediaCast.podspec). 22.3.1 verified latest stable on 2026-08-13 against
# Google Maven's group index
# (https://dl.google.com/android/maven2/com/google/android/gms/group-index.xml).
# Exact pin on purpose: Google has shipped discovery-breaking patch releases
# (iOS 4.8.0/4.8.1); the upstream-currency watcher
# (scripts/check-upstream.mjs) tracks this row so a lag is loud, not silent.
RnMediaCast_castFrameworkVersion=22.3.1
