///
/// CastPlayerState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::rnmediacast {

  /**
   * An enum which can be represented as a JavaScript union (CastPlayerState).
   */
  enum class CastPlayerState {
    IDLE      SWIFT_NAME(idle) = 0,
    UNKNOWN      SWIFT_NAME(unknown) = 1,
    LOADING      SWIFT_NAME(loading) = 2,
    BUFFERING      SWIFT_NAME(buffering) = 3,
    PAUSED      SWIFT_NAME(paused) = 4,
    PLAYING      SWIFT_NAME(playing) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::rnmediacast

namespace margelo::nitro {

  // C++ CastPlayerState <> JS CastPlayerState (union)
  template <>
  struct JSIConverter<margelo::nitro::rnmediacast::CastPlayerState> final {
    static inline margelo::nitro::rnmediacast::CastPlayerState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"): return margelo::nitro::rnmediacast::CastPlayerState::IDLE;
        case hashString("unknown"): return margelo::nitro::rnmediacast::CastPlayerState::UNKNOWN;
        case hashString("loading"): return margelo::nitro::rnmediacast::CastPlayerState::LOADING;
        case hashString("buffering"): return margelo::nitro::rnmediacast::CastPlayerState::BUFFERING;
        case hashString("paused"): return margelo::nitro::rnmediacast::CastPlayerState::PAUSED;
        case hashString("playing"): return margelo::nitro::rnmediacast::CastPlayerState::PLAYING;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum CastPlayerState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::rnmediacast::CastPlayerState arg) {
      switch (arg) {
        case margelo::nitro::rnmediacast::CastPlayerState::IDLE: return JSIConverter<std::string>::toJSI(runtime, "idle");
        case margelo::nitro::rnmediacast::CastPlayerState::UNKNOWN: return JSIConverter<std::string>::toJSI(runtime, "unknown");
        case margelo::nitro::rnmediacast::CastPlayerState::LOADING: return JSIConverter<std::string>::toJSI(runtime, "loading");
        case margelo::nitro::rnmediacast::CastPlayerState::BUFFERING: return JSIConverter<std::string>::toJSI(runtime, "buffering");
        case margelo::nitro::rnmediacast::CastPlayerState::PAUSED: return JSIConverter<std::string>::toJSI(runtime, "paused");
        case margelo::nitro::rnmediacast::CastPlayerState::PLAYING: return JSIConverter<std::string>::toJSI(runtime, "playing");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert CastPlayerState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"):
        case hashString("unknown"):
        case hashString("loading"):
        case hashString("buffering"):
        case hashString("paused"):
        case hashString("playing"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
