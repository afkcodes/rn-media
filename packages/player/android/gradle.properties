RnMediaPlayer_kotlinVersion=2.2.0
# Matches the RN 0.87 template floor; standalone consumers get the same minimum
# the example app (and the prebuilt libmpv audio builds) are validated against.
RnMediaPlayer_minSdkVersion=24
RnMediaPlayer_targetSdkVersion=35
RnMediaPlayer_compileSdkVersion=37
RnMediaPlayer_ndkVersion=27.1.12297006
