# @afkcodes/timbre-audio-session

## 0.1.0

### Minor Changes

- Initial release. Audio-focus arbiter: focus acquisition and loss, ducking,
  interruptions, becoming-noisy (headset unplug), and route changes — over
  Android's `AudioManager` and iOS's `AVAudioSession`. Android device-verified;
  iOS written and CI-compiled. Pre-1.0 — the API may still change.
