# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# The New Architecture is the only architecture this project supports, and since React Native 0.82
# `newArchEnabled=false` is rejected by the RN Gradle plugin anyway. The line is kept ONLY because
# react-native-nitro-modules 0.36.5's own android/build.gradle still gates its new-arch setup on
# `rootProject.hasProperty("newArchEnabled") && == "true"` — removing it silently drops Nitro into
# its legacy branch. Revisit when Nitro drops that check.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
#
# The RN 0.87 template flips this to `true`; we deliberately do NOT follow it.
# Edge-to-edge is a layout choice for the demo UI, not a 0.87 requirement — the
# flag is still read by the Gradle plugin in 0.87 (PropertyUtils.kt,
# EDGE_TO_EDGE_ENABLED), so `false` remains a supported value. Flipping it would
# push the example's content under the status bar and turn a dependency bump
# into a UI change, which would muddy the device verification this bump is
# gated on. Revisit as its own change. (2026-08-17, RN 0.87.0 evaluation.)
edgeToEdgeEnabled=false

# Opt out of built-in Kotlin and the new DSL that ship with AGP 9 — required by
# the RN 0.87 template, which is the first release to build on AGP 9 (9.2.1, per
# @react-native/gradle-plugin@0.87.0 gradle/libs.versions.toml). Without these,
# AGP 9 applies its own Kotlin plugin and DSL semantics on top of the
# `org.jetbrains.kotlin.android` plugin our library modules already apply, and
# the two conflict. Starting from AGP 10.x these opt-outs are removed and the
# library modules will have to migrate to the new DSL.
android.builtInKotlin=false
android.newDsl=false
