/**
 * The React Native entry point (`index.js` registers this) — deliberately a
 * one-liner.
 *
 * Everything real is under `src/`, which is where a reader should start:
 * `src/App.tsx` is the composition root and its header maps the rest of the
 * tree. This file exists only so the RN scaffold's `import App from './App'`
 * keeps resolving; keeping it thin is what lets the app be reorganised without
 * touching the native projects.
 */
export { default } from './src/App'
